interface:
  default_prompt: "Only use $tauri-mcp-cli when the user explicitly requests tauri-mcp screenshot recognition; reuse an already-running debug instance instead of starting another one."
  display_name: "Tauri MCP Screenshot Recognition"
  short_description: "Screenshot recognition on explicit request only"
